/**
 * @param {string} s
 * @param {number[][]} queries
 * @return {boolean[]}
 */

var psum = new Array(32);

var build = function (s) {
    for(var i=0; i<27; i++) psum[i] = new Array(100007);
    for(var i=0; i<s.length; i++) {
        var ch = s[i];
    }
}

var canMakePaliQueries = function(s, q) {

};